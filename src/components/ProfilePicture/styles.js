import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 43,
    borderWidth: 3,
    borderColor: '#B9E0CF',
  },
  image: {
    borderRadius: 40,
    borderWidth: 1,
    borderColor: '#fff',
  },
});
